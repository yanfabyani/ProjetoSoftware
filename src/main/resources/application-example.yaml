# Arquivo de exemplo de configuração
# Copie este arquivo para application.yaml e ajuste as credenciais

spring:
  application:
    name: rede-mais-social
  
  datasource:
    url: jdbc:mysql://localhost:3306/rede_mais_social?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: clau2835
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: seu_email@gmail.com
    password: sua_senha_app
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

